## Agent skills

### Issue tracker

Issues live in GitHub Issues (via `gh`) — not yet wired up since this repo has no git remote configured yet. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root, created lazily as needed). See `docs/agents/domain.md`.
